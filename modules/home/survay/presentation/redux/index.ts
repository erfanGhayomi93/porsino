import { createSlice } from '@reduxjs/toolkit'
  
export const survaySlice = createSlice({
    name: 'survay',
    initialState: {},
    reducers: {},
})

export const {} = survaySlice.actions

export default survaySlice.reducer;

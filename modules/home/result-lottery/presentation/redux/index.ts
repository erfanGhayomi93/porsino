import { createSlice } from '@reduxjs/toolkit'
  
export const resultLotterySlice = createSlice({
    name: 'resultLottery',
    initialState: {},
    reducers: {},
})

export const {} = resultLotterySlice.actions

export default resultLotterySlice.reducer;

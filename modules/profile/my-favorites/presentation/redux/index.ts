import { createSlice } from '@reduxjs/toolkit'
  
export const myFavoritesSlice = createSlice({
    name: 'myFavorites',
    initialState: {},
    reducers: {},
})

export const {} = myFavoritesSlice.actions

export default myFavoritesSlice.reducer;

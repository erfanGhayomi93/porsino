import { createSlice } from '@reduxjs/toolkit'
  
export const mySurvaySlice = createSlice({
    name: 'mySurvay',
    initialState: {},
    reducers: {},
})

export const {} = mySurvaySlice.actions

export default mySurvaySlice.reducer;

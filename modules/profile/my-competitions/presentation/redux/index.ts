import { createSlice } from '@reduxjs/toolkit'
  
export const myCompetitionsSlice = createSlice({
    name: 'myCompetitions',
    initialState: {},
    reducers: {},
})

export const {} = myCompetitionsSlice.actions

export default myCompetitionsSlice.reducer;

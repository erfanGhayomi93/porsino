import { createSlice } from '@reduxjs/toolkit'
  
export const bankPorsinoSlice = createSlice({
    name: 'bankPorsino',
    initialState: {},
    reducers: {},
})

export const {} = bankPorsinoSlice.actions

export default bankPorsinoSlice.reducer;

import { createSlice } from '@reduxjs/toolkit'
  
export const newCompetitionSlice = createSlice({
    name: 'newCompetition',
    initialState: {},
    reducers: {},
})

export const {} = newCompetitionSlice.actions

export default newCompetitionSlice.reducer;

import { createSlice } from '@reduxjs/toolkit'
  
export const newSurvaySlice = createSlice({
    name: 'newSurvay',
    initialState: {},
    reducers: {},
})

export const {} = newSurvaySlice.actions

export default newSurvaySlice.reducer;
